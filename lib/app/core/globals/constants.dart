import 'package:flutter/material.dart';

class CustomColors {
  static const Color primary = Color(0xFF0EC00B);
  static const Color background = Color(0xFF0B0B0B); //any text color
  static  const  Color greybackground = Color(0xFF1A1A1A);
   static  Color textgrey = Colors.white.withOpacity(0.45);
  static const Color white = Colors.white; 
}
